<?php

class TapsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('taps')->truncate();

		$taps = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('taps')->insert($taps);
	}

}
